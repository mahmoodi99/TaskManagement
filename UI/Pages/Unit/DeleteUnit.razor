@page "/DeleteUnit/{Id}"
@using Share.Dto;

<h2>حذف سمت</h2>
@if (Unit != null)
{
    <p>آیا از حذف این سمت مطمعن هستید؟ <b>@Unit.Title</b></p>
    <div class="row">
        <table class="table">
            <tr>
                <td>پرژه</td>
                <td>@Unit.Title</td>                       
                <td>عنوان</td>
                <td>@Unit.Description</td>
            </tr>
        </table>
    </div>
}
<div class="row">
    <div class="col-md-4">
        <div class="form-group">
            <input type="button" class="btn btn-success" @onclick="@(() => Delete(Unit.Id))" value="حذف" />
            <input type="button" class="btn btn-danger" @onclick="@Cancel" value="انصراف" />
        </div>
    </div>
</div>
